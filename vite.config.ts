import { defineConfig } from 'vite';

export default defineConfig({
  base: '/enviro-assignment/', // My profiler epository name on git Hub Page
  plugins: [],
  server: {
    port: 3000, //  desired port
  },
});
